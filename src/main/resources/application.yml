spring:
  datasource:
    username: liu
    password: root
    url: jdbc:mysql://175.24.91.236:3306/curriculum_design?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  devtools:
    restart:
      enabled: true
  jackson:
    date-format: yyyy-MM-dd
    time-zone: GMT+8
server:
  tomcat:
    uri-encoding: UTF-8
  port: 8080

